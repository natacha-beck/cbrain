en:

  custom_filters:

    common:
      filtering_by_date: "Filtering by date"
      archiving_status: "Archiving status"
      owners: "Owners"
      dont_filter_description: "Don't filter description"

      match_type:
        match: "Matches"
        match_exactly: "Match exactly"
        contain: "Contains"
        begin: "Begins with"
        end: "Ends with"

      archiving:
        dont_filter: "Don't filter by archiving status"
        archived: "Archived"
        not_archived: "Not archived"
        on_cluster: "Archived on cluster"
        as_file: "Archived as file"

      userfile:
        filename: "Filename"
        dont_filter_name: "Don't filter name"
        parent_name_contains: "Parent name contains"
        lists_children: "(lists children)"
        child_name_contains: "Child name contains"
        lists_parents: "(lists parents)"
        dont_filter_size: "Don't filter size"
        synchronization_status: "Synchronization status"

      task:
        work_directory_status: "Work Directory status"
        wd_status:
          dont_filter: "Don't filter by work directory status"
          shared: "Use another task's"
          not_shared: "Have own Work Directory"
          exists: "Work Directory exists on cluster"
          none: "Work Directory does not exist on cluster"

    custom_filter_li:
      edit_delete: "Edit/Delete"

    custom_filter_list:
      by_custom_filter: "By Custom Filter"
      create_custom_filter: "Create Custom Filter"

    new_userfile_custom_filter:
      file_types: "File types"

    new_task_custom_filter:
      task_types: "Task types"

    new:
      title: "New %{type}"
      filter_name: "Filter name"
      custom_filter: "custom filter"

    task_custom_filter:
      header:
        types: "Types"

    userfile_custom_filter:
      header:
        by_file_types: "By file types"

    show:
      update_error: "custom filter could not be updated."
